Initial release of the MySQL Plugin.

- Add functionality to pull from an external MySQL database
- Return either single value or full row based on Return Single Value input (`YES`, `NO`)
- Add functionality to INSERT data into an external MySQL database